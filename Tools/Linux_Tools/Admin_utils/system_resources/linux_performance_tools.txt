
https://www.thegeekstuff.com/2011/03/linux-performance-monitoring-intro/
https://www.thegeekstuff.com/2011/12/linux-performance-monitoring-tools/
-------------------------------------------------
Linux Performance Tools, Brendan Gregg, part 1 of 2
https://www.youtube.com/watch?v=FJW8nGV4jxY
https://www.youtube.com/watch?v=zrr2nUln9Kk

https://www.brendangregg.com/blog/2017-08-08/linux-load-averages.html
https://www.brendangregg.com/systems-performance-2nd-edition-book.html

-------------------------------------------------
https://www.redhat.com/en/blog/troubleshooting-slow-servers

##The "Big 3" (aka CPU, RAM, and Disk I/O)
--Checking all of the "Big 3" with sar

sar -u gives you info about all CPUs on the system, starting at midnight:
sar -r, which give you that day’s memory usage
sar -d, which gives you the disk I/O output using just the device name. To get the name of the devices, use sar -dP

If any of these commands show a problem, you can go back to see when the server issues started by using sar {-u, -r, -d, -dP} -f /var/log/sa/sa<XX> (where XX is the day of the month you wish to look for).

-------------------------------------------------
#vmstat

vmstat -Sm 1

vmstat -S M -n 1 5

-------------------------------------------------
##IOStats
monitors real-time disk I/O performance on Linux in megabytes per second, updating every second while filtering out idle drives.

iostat -xmdz 1

=============================================================================================================
Flag |Meaning             |Description
=============================================================================================================
-x   |Extended statistics |Displays detailed performance metrics like latency, queue size, and saturation.
-m   |Megabytes/sec       |Formats read/write throughput in MB/s instead of kilobytes or sectors.
-d   |Device only         |Displays the disk device report and hides the CPU utilization section.
-z   |Omit idle           |Hides drives with zero activity during the sample interval to keep output clean.
1    |1-second interval   |Refreshes the metrics continuously every 1 second (press Ctrl+C to stop).

=============================================================================================================

Key Metrics to Watch
When inspecting the output, keep an eye on these specific columns:
    • r/s & w/s: Read and write requests per second (IOPS).
    • rMB/s & wMB/s: Read and write throughput in MB/s.
    • avgqu-sz: Average queue length of requests waiting for the drive. A value consistently greater than 1 or 2 per disk channel signals a bottleneck.
    • await: The average time (in milliseconds) an I/O request spent waiting in the queue plus being serviced.
        ◦ HDDs: $> 10\text{–}15\text{ ms}$ indicates lag.
        ◦ NVMe/SATA SSDs: $> 1\text{–}2\text{ ms}$ indicates lag.
    • %util: Percentage of time the disk was actively handling requests. Near $100\%$ means the drive is working at capacity (though fast NVMe SSDs or RAID arrays can handle parallel queues even at high %util).

-------------------------------------------------
##linux tools to trace application delays retrospectively 

sysstat runs in the background as a system daemon and collects system-wide counters at regular intervals (typically every 1–10 minutes) stored in binary logs (/var/log/sysstat/saDD).

    What it catches retrospectively:
    System-wide CPU load spikes, 
	context switch storms (sar -w), 
	memory swapping (sar -r), disk 
	IOPS/saturation (sar -d), and 
	network bandwidth bounds (sar -n DEV).

Example Retrospective Query:

# Check system-wide context switches and run-queue saturation between 14:00 and 14:30 today
sar -q -w -s 14:00:00 -e 14:30:00
-------------------------------------------------
## linux tools to trace application delays retrospectively

trace which process was blocking system resources 
can IO delays be caused due to context switching 

computer performance design, evaluation, analysis, and tuning.
tools, methodologies, and visualizations for performance analysis,

-------------------------------------------------
##does the JVM manage thread scheduling of java processes or does the os manage scheduling 

How Java Interacts with OS Scheduling

While the JVM delegates scheduling to the kernel, it provides mechanisms to interface with the OS scheduler:

    Thread Priorities (Thread.setPriority()):

        Java thread priorities (1 through 10) act as hints to the OS scheduler.

        On Linux, the JVM maps these Java priorities to Linux nice values. However, how strongly these hints are respected depends entirely on the OS policy (and root/capability permissions).

    Voluntary Yielding (Thread.yield()):

        Calling yield() informs the OS scheduler that the current thread is willing to give up its CPU time slice, prompting a voluntary context switch (cswch).

What About Java 21+ Virtual Threads (Project Loom)?

With Java 21 and Virtual Threads, the story gets a two-layer dynamic:

[ Virtual Threads ]   --> Managed by JVM (M:N mapping)
        |
        v
[ Carrier Threads ]   --> Standard Platform Threads (1:1 with OS)
        |
        v
[ OS Kernel Threads ] --> Managed by OS Scheduler

    Virtual Threads (JVM-managed): The JVM manages these lightweight threads at the application level. When a virtual thread performs a blocking I/O operation (or enters a wait state), the JVM unmounts it from its underlying OS thread without triggering an expensive OS context switch.

    Carrier / Platform Threads (OS-managed): The underlying pool of worker threads ("carrier threads") that actually execute virtual threads are standard platform threads—and those carrier threads are still scheduled and preempted by the OS kernel.

-------------------------------------------------
1. How High Context Switching Causes I/O Delays
When context switching spikes excessively (a "context switch storm"), it creates severe system overhead that degrades I/O performance in two main ways:
    • CPU Cache Trashing (L1/L2/L3 Caches): When the kernel rapidly switches execution between many different threads, CPU caches and Translation Lookaside Buffers (TLB) are constantly invalidated and flushed. When a thread finally gets CPU time to process I/O data (e.g., handling incoming disk/network buffer data), it suffers massive cache misses. This slows down the CPU's ability to process I/O submission/completion queues, inflating overall await times.
    • Kernel & Lock Contention: Context switches aren't free—they execute kernel code (the scheduler). High context switching burns CPU cycles in system mode (%sys). If threads are constantly switching while holding or requesting file-system locks, block layer locks, or device driver locks, other threads waiting to perform I/O get blocked, directly causing elevated iodelay.

2. How I/O Causes Context Switching (The Reversal)
More commonly, high voluntary context switches are a symptom of an application waiting on slow I/O:
    • Blocking I/O Calls: When a thread issues a synchronous read or write to a disk or network socket, it cannot proceed until the data is ready. The kernel puts the thread into a sleeping/waiting state ($D$ state for disk, $S$ state for network) and performs a voluntary context switch (cswch) to give the CPU core to another runnable thread.
    • High-Frequency Small I/O Operations: If an application performs thousands of unbuffered, tiny reads/writes per second, every single request causes the thread to yield the CPU, wait for the storage controller, and wake back up—causing context switch rates and I/O latency to spike together.

-------------------------------------------------
##system latency causes

=====================
page faults
context switching
IO delay
network errors/retransmissions
=====================

Quick Diagnostics Cheat-Sheet
===============================================================================
Cause	            | Key Metrics to Check	        | Recommended Tool
===============================================================================
I/O Bottlenecks	    | %iowait, await, %util	        | sar -d, iostat -xz 1
Memory Reclaim	    | allocstall, pgscand	        | sar -B, /proc/vmstat
CPU Runqueue Delay	| runq-sz, run-queue latency	| sar -q, perf sched
Hardware SMIs	    | SMI counts, latency spikes	| hwlatdetect, cyclictest
Cgroup Throttling	| nr_throttled, throttled_time  | /sys/fs/cgroup/.../cpu.stat
===============================================================================

Here are the primary causes of Linux system latency, categorized by the subsystem involved:

1. Storage & Disk I/O Bottlenecks
    I/O Saturated Queues (I/O Wait): When processes are waiting for synchronous disk reads/writes to complete, they enter an uninterruptible sleep state (D state). Slow disks, bad controllers, or high write loads (e.g., heavy database flushes) quickly inflate tail latency.
    File System Locks & Journaling: Heavy journaling in filesystems like ext4 or file-locking mechanisms (e.g., flock, fcntl) force processes to wait for metadata writes to clear before proceeding.
    Buffer Cache Thrashing: If the kernel is constantly clearing and re-populating page cache memory because working sets exceed RAM size, disk reads spike.

2. Memory Contention & Allocation
    Direct Memory Reclaim: When available free memory drops below the vm.min_free_kbytes watermark, the kernel pauses process allocations to reclaim memory synchronously, adding severe latency spikes.
    Transparent Huge Pages (THP) Defrag: When THP is enabled in always mode, the kernel’s khugepaged daemon allocates 2 MB huge pages on the fly, triggering memory compaction and synchronous locks.
    NUMA Remote Access: On multi-socket NUMA systems, if a process running on CPU Socket 0 accesses memory physically attached to Socket 1 (remote node), memory access latency significantly increases.

3. CPU Scheduling & Lock Contention
    CPU Core Saturation & Runqueue Latency: When the CPU runqueue is flooded (high load average relative to available cores), runnable processes sit waiting for an available time slice.
    Kernel Spinlocks and Mutexes: High concurrency can lead to lock contention in the kernel or user space, where threads spend time spinning or sleeping while waiting to acquire a lock.
    CPU Throttling (C-States & P-States):
        Dynamic Frequency Scaling: Deep sleep states (C-states) save power but introduce latency when waking a core back up.
        Thermal Throttling: CPU hardware drops clock speeds when temperature thresholds are hit.
    Cgroups / Container Throttling: If running inside Docker/Kubernetes, hitting CFS (Completely Fair Scheduler) CPU quotas (cpu.cfs_quota_us) will cause the kernel to forcibly throttle execution, introducing artificial tail latency.

4. Network Subsystem Delays
    Packet Drop & Buffer Bloat: Overfilled network device queues (TX/RX ring buffers) lead to packet drops, forcing expensive TCP retransmissions.
    TCP SYN Backlog Overflows: If the application socket backlog is full, incoming connections are dropped or delayed during the TCP handshake.
    NAPI / SoftIRQ Overload: High packet rates process interrupts on CPU Core 0 or spread across cores via softirqd. If ksoftirqd maxes out CPU cores, packet processing halts.
    DNS Resolution Delays: Synchronous or slow DNS lookups inside the network path block application execution threads.

5. Hardware & Firmware Level Delays
    System Management Interrupts (SMIs): Motherboard BIOS/firmware executes SMIs for hardware management (e.g., fan speed, power control). SMIs pause the entire OS, including the hypervisor/kernel, completely out of Linux’s control (often called "Hardware Latency").
    PCIe Bus Bottlenecks: Saturation on PCIe lanes when transferring data between NVMe drives, GPUs, and NICs.
    Interrupt Storms: Misconfigured hardware or failing devices generating excessive hardware interrupts (IRQs) that consume all CPU processing capacity.


-------------------------------------------------
-------------------------------------------------
https://medium.com/@bektiaw
https://medium.com/@bektiaw/13-tiny-bash-scripts-that-save-me-time-every-day-40b09fb61a06
https://dev.to/numbpill3d/20-practical-bash-scripts-for-everyday-automation-2026-edition-3l7i
https://evanhahn.com/scripts-i-wrote-that-i-use-all-the-time/
https://www.tecmint.com/bash-scripts-linux-sysadmin/

https://medium.com/@bektiaw/linux-kernel-tunables-to-boost-performance-and-stability-6267028fd0ca
https://medium.com/@bektiaw/how-to-investigate-disk-i-o-bottlenecks-e6810b2511e3
https://monzool.net/blog/2024/07/14/a-second-search-for-bash-scripting-alternatives/


-------------------------------------------------
-------------------------------------------------
-------------------------------------------------
-------------------------------------------------
-------------------------------------------------
-------------------------------------------------
-------------------------------------------------
